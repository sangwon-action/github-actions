 name: issue-keyword-notify

on:
  issues:
    types: [opened]

jobs:
  get-keyword:
    runs-on: ubuntu-latest
    if: |
      job level에서 contains사용 
    outputs:
      level: ${{ steps.issue-title.outputs.level }}
    steps:
    - name: create output
      id: keyword
      run: |
        # 키워드가 critical -> output level=critical
        # 키워드가 normal -> output level=normal

  slack:
    needs: [get-keyword]
    runs-on: ubuntu-latest
    environment: 아웃풋 level 사용 
    steps:
    - name: slack 
      uses: slackapi/slack-github-action@v1.24.0
      with:
        payload: |
          # 생략
      env:
        SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        SLACK_WEBHOOK_TYPE: INCOMING_WEBHOOK
